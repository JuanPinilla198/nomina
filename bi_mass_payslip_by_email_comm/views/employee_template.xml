<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--Email template -->
        <record id="email_template_edi_hr_payroll" model="mail.template">
            <field name="name">Employee - Send Payslip by mail</field>
            <field name="subject"> Desprendible de Nómina Ref. ${object.number} - ${object.company_id.name}</field>
            <field name="email_from">${object.employee_id.user_id.partner_id.email}</field>
            <field name="email_to">${object.employee_id.work_email}</field>
            <field name="partner_to">${object.employee_id.user_id.partner_id.id}</field>
            <field name="model_id" ref="bi_hr_payroll.model_hr_payslip"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.employee_id.lang}</field>
            <field name="report_template" ref="eq_payslip_custom_pdf_report.custom_payslip_report"/>
            <field name="report_name">${(object.name or '').replace('/','-')}</field>
            <field name="body_html"><![CDATA[
                    <div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">
                        <p>Buen dia ${object.employee_id.name}, </p>
                        <p>En este correo encontrarás un adjunto de tu desprendible de Nómina. <br/><br/>
                            <b>Referencia:</b> ${object.number}.<br/>
                            <b>Desde:</b> ${object.date_from} <br/>
                            <b>Hasta:</b> ${object.date_to}<br/>
                            <b>Valor:</b> $ ${"%.2f" % object.total_payslip}<br/></p>
                        <p>Si tiene alguna pregunta, no dude en ponerse en contacto con RRHH</p><br/>
                        <p>Gracias.</p>
                        <p>Departmento RRHH.</p>
                        <p>${object.company_id.name}</p>
                    </div>
                    ]]></field>
        </record>


    </data>
</odoo>
