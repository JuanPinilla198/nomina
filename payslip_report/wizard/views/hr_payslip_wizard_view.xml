<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Wizard view-->
        <record  model="ir.ui.view" id="ir_ui_payslip_report_wizard_view">
            <field name="name">Payslip Report Wizard</field>
            <field name="model">payslip.report.wizard</field>
            <field name="arch" type="xml">
                <form string="Payslip Report Wizard">
                    <group invisible="1">
                        <field name="state"/>
                        <field name="name"/>
                        <field name="payslip_ids"/>
                    </group>
                    <group >
                        <group string="Filtro por fechas">
                            <field name="date_from" string="Desde"/>
                            <field name="date_to" string="Hasta"/>
                        </group>
                        <group>
                        </group>
                    </group>
                    <group>
                        <group string="Filtro por cuentas">
                            <field name="analitic_account_city" string="Ciudad" widget="many2many_tags" options="{'no_open':True, 'no_create': True,'no_quick_create':True,'no_create_edit':True}"/>
                            <field name="analitic_account_company" string="Empresa" widget="many2many_tags" options="{'no_open':True, 'no_create': True,'no_quick_create':True,'no_create_edit':True}"/>
                        </group>
                        <group>
                        </group>
                    </group>
                    <group>
                        <field name="xls_file" readonly="1" filename="name" states="download"/>
                    </group>
                    <footer>
                        <button name="print_report_xls" string="Generar Excel" type="object" class="oe_highlight" states="choose"/>
                        <button name="print_report_pdf" string="Generar PDF" type="object" class="oe_highlight" states="choose"/>
                        <button name="action_go_back" string="Regresar" type="object" class="oe_highlight" states="download"/>
                        or
                        <button string="Cerrar" special="cancel" class="btn-primary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_wizard_payslip_report_excel" model="ir.actions.act_window">
            <field name="name">Payslip Report</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">payslip.report.wizard</field>
           
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <!-- TODO: check if we need this -->
            <field name="binding_model_id" ref="bi_hr_payroll.model_hr_payslip" />
        </record>


    </data>

    
</odoo>
